apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: pulumi-kubernetes-operator

resources:
  - ../../base/pulumi-operator

patches:
  - path: helmrelease-patch.yaml
    target:
      kind: HelmRelease
      name: pulumi-kubernetes-operator
